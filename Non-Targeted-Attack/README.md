# Non Targeted Attack Solution

## Main Strategy
* Ensemble model of inception, resnet(adversarial trained), randomization layer, Defense-GAN, logit pairing, and denoising layer using momentum-based PGD


